// 🤖 INTERNAL NOTE:
// Purpose: Purchase-related route controllers for events and sessions
// Exports: router (default)
// Feature: purchases
// Dependencies: drizzle ORM, purchaseTransactions table

import { Router } from "express";

const router = Router();

export default router;